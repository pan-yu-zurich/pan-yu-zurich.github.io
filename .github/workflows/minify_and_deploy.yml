name: Build and Deploy

on:
  push:
    branches:
      - gh-pages
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

defaults:
  run:
    shell: bash

jobs:
  build-and-minify:
    runs-on: ubuntu-latest
    env:
      HUGO_VERSION: "0.162.1"
      HUGO_ENVIRONMENT: "production"
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "16"

      - name: Install theme dependencies
        run: |
          npm install

      - name: Setup Hugo
        uses: peaceiris/actions-hugo@v2
        with:
          hugo-version: "0.162.1"
          extended: true

      - name: Final build
        run: |
          set -o pipefail
          hugo --destination docs 2>&1 | tee build.log || { echo "::error::HUGO_FAILED $(grep -iE 'error|denied|failed' build.log | head -n 8 | tr '\n' '|' | head -c 900)"; exit 1; }

      - name: Upload artifacts
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./docs

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build-and-minify
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
